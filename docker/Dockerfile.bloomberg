FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx python-dateutil

COPY bloomberg_proxy.py .

EXPOSE 6901

CMD ["uvicorn", "bloomberg_proxy:app", "--host", "0.0.0.0", "--port", "6901"]
